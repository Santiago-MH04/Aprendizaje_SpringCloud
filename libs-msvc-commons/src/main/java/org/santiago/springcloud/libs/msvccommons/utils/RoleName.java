package org.santiago.springcloud.libs.msvccommons.utils;

import lombok.Getter;

@Getter
public enum RoleName {
        //Constantes en RoleName
    ROLE_ADMIN,
    ROLE_USER;

    //Atributos de RoleName
    //Constructores de RoleName
    //Asignadores de atributos de RoleName (setters)
    //Lectores de atributos de RoleName (getters)
    //Métodos de RoleName
    
}
